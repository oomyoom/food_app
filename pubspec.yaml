name: food_app
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.0.5 <4.0.0"

dependencies:
  carousel_slider: ^4.2.1
  flutter:
    sdk: flutter
  flutter_launcher_icons: ^0.13.1
  flutter_speed_dial: ^7.0.0
  http: ^1.1.0
  image_cropper: ^5.0.0
  image_picker: ^1.0.2
  stripe_checkout: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/logo_app(crop).jpg"

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/font/
